/**
 * @vanduo-oss/vd3 — Vanduo UI for Vue 3 (the vd3 line).
 *
 * Phase 0 scaffold: the public surface is intentionally minimal. The DTCG
 * token pipeline and CSS tree land in the `vd3-token-css-foundation` change;
 * components, composables and the theme API land in the `vd3-carryover`
 * change.
 */
export const VD3_VERSION = "0.1.0";
